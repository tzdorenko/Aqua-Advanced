const number = 5;
for (let i = 1; i <= 10; i++) {
    let row = "";
    console.log (number + "*" + i + "=" + (number*i) );
    }

    let j = 1;
    while (j <= 10) {
        console.log (number + "*" + j + "=" + (number *j));
        j++
    }